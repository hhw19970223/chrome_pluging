module HHW {
    export class HAction {
        /** 鼠标是否按下 */
        private _isDown: boolean;

        constructor() {
            this._isDown = false;
        }

        public onListen(event) {

        }
    }
}